begin_problem(template).

list_of_descriptions.
name({*Template*}).
author({*Le Thesard Inconnu*}).
status(unsatisfiable).
description({*Just a stupid template*}).
end_of_list.

list_of_symbols.
    predicates[(P,1),(Q,1)].
end_of_list.

list_of_formulae(conjectures).

formula(

exists([x], forall([y], forall([z], implies(implies(implies(P(y), Q(z)), P(x)), Q(x)))))

).

end_of_list.

end_problem.

